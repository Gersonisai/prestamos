# Zénit – Gestión de Préstamos (Auth local + tasa variable + Excel)

## Autenticación local
- Usuario: **Iblis**  
- Contraseña: **Iblis04**  
La sesión se guarda en **localStorage** del navegador (no requiere servidor).

## Ejecutar en local
```bash
npm install
npm run dev
```

## Despliegue en Vercel
- Build Command: `npm run build`
- Output Directory: `dist`
```
